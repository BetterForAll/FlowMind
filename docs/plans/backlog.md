# Backlog

Features we want to build later, not now.

<!--
Moved out of backlog on 2026-04-17:

- "Flow deduplication / merging" → now tracked as active issue #1
  "Evaluator pass + auto-resolver (cross-session flow matching / evidence accumulation)".
  See the active work item in the session plan; implementation spans
  `src/engine/flow-detection.ts`, `src/engine/flow-store.ts`, and a new
  `src/engine/evaluator.ts` (separate Gemini call, Level 2 matching primary,
  Level 1 within-run sanity check secondary).
-->

<!-- No backlog items currently parked. Add new deferred work here. -->
